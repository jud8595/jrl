// export type Ticket = {
//     id: string;
//     description: string;
// }

// hack: why not having JSON.stringify(json) so that we work we json and display like text
export type Ticket = String[];